<script>export let data;</script>
<svelte:head><title>{data.post.title} — Swartzit</title></svelte:head>
<header><a class="brand" href="/">swartzit</a><span>Read freely. Participate under a pseudonym. Take your community with you.</span><a href="/api/export?community={data.post.community}" download="swartzit-community-export.json">Export community</a><a class="login" href="/login">Sign in</a></header>
<main class="post-page"><a class="back" href="/?community={data.post.community}">← c/{data.post.community}</a><article class="post"><div class="meta"><span>c/{data.post.community}</span><span>·</span><span>posted by u/{data.post.author}</span><span>·</span><time datetime={data.post.created_at}>{new Date(data.post.created_at).toLocaleDateString()}</time></div><h1>{data.post.title}</h1><p>{data.post.body}</p><footer>↕ 1 · {data.comments.length} comments</footer></article><section class="comments"><h2>Comments</h2>{#if data.comments.length === 0}<p class="empty">No comments yet.</p>{:else}{#each data.comments as comment}<article class:reply={comment.parent_id !== null}><div class="meta">u/{comment.author}<span>·</span><time datetime={comment.created_at}>{new Date(comment.created_at).toLocaleDateString()}</time></div><p>{comment.body}</p></article>{/each}{/if}{#if data.comments_truncated}<p class="empty">Some older comments are not shown in this view.</p>{/if}</section></main>
