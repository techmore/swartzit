# Optional drop-in for an Ubuntu Caddy edge whose public listener lives on a
# WireGuard address. Install this only after /etc/wireguard/wg0.conf exists.
[Unit]
Requires=wg-quick@wg0.service
After=network-online.target wg-quick@wg0.service
PartOf=wg-quick@wg0.service
