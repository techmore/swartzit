# Optional drop-in for an Ubuntu web service whose public edge reaches it over
# WireGuard. Install this only after /etc/wireguard/wg0.conf exists.
[Unit]
Requires=wg-quick@wg0.service
After=network-online.target wg-quick@wg0.service
PartOf=wg-quick@wg0.service
