# Replace the hostname with the operator’s domain. Onion-only hosts do not use
# this file; Tor forwards directly to the website at 127.0.0.1:4173 instead.
example.org {
    encode gzip zstd
    reverse_proxy 127.0.0.1:4173
}
